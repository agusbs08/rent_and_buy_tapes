@model IEnumerable<ProjectUAS.Models.Pembatalan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_pemesanan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.denda)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.refund)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_pemesanan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.denda)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.refund)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
